kind: pipeline
name: docker
name: default
steps:
 - name: scp files
   image: appleboy/drone-scp
   settings:
      host: 52.91.230.185
      user: ubuntu
      key:
        from_secret: ssh-key        
      port: 22
      target: home/
      source: demo.txt

      
